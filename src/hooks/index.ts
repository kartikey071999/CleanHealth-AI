// Custom React Hooks

// Example hook - add your custom hooks here
export const useLocalStorage = <T,>(key: string, initialValue: T) => {
  // Hook implementation
};

export const useDebounce = <T,>(value: T, delay: number) => {
  // Hook implementation
};

// Add more hooks as needed
